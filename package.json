{
  "name": "growgently-books",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply growgently-books --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
